# Security Policy

## 설계상 금지된 기능

이 저장소는 업비트 공개 시세만 읽습니다. 다음 기능은 구현하지 않습니다.

- 주문 생성·취소·조회
- 자산·잔액 조회
- 입금·출금
- 업비트 Access Key/Secret Key 또는 JWT 생성
- 사용자가 제공한 URL에 대한 임의 요청

## 비밀정보 처리

저장소에는 비밀정보를 커밋하지 않습니다. `.env`는 Git에서 제외됩니다. GitHub 연결 토큰과 Railway 배포 토큰은 해당 플랫폼의 보안 저장소에서만 관리해야 합니다.

현재 서버는 공개 데이터 전용이므로 업비트 자격증명이 전혀 필요하지 않습니다. 누군가 업비트 키를 요청하면 제공하지 마십시오.

## 네트워크 경계

클라이언트는 `https://api.upbit.com`만 허용하며 리디렉션을 따르지 않습니다. 마켓 코드는 `KRW-자산코드` 형식으로 검증하고, 호출 경로는 코드의 허용 목록에서만 선택합니다. 이로써 사용자 입력을 통한 SSRF와 비공개 API 접근을 줄입니다.

## 취약점 신고

민감한 정보는 공개 이슈에 올리지 말고 저장소 소유자에게 GitHub의 비공개 보안 신고 기능으로 전달하십시오.
